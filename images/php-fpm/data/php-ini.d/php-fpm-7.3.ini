; ############################################################
; # Workspace PHP defaults for 7.3-fpm
; ############################################################

[PHP]

; Memory
; Note: "memory_limit" should be larger than "post_max_size"
memory_limit = 768M

; Setup opcache configuration
opcache.enable = 1
opcache.validate_timestamps = 1

; User-ini configuration
user_ini.filename = 
